@model IEnumerable<LainausjarjestelmaMVC.Models.Logins>

@{
    ViewBag.Title = "Loginit";
}

<br />

<h2>Loginit</h2>

<p>
    @Html.ActionLink("LISÄÄ UUSI", "Create", "Logins", new { @class = "btn btn-primary btn-sm" })
</p>

<table class="table table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salasana)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Admin)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salasana)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Admin)
            </td>
            <td>
                @Html.ActionLink("MUOKKAA", "Edit", new { id = item.LoginID }, new { @class = "btn btn-secondary btn-sm" })&nbsp;
                @Html.ActionLink(" POISTA ", "Delete", new { id = item.LoginID }, new { @class = "btn btn-outline-danger btn-sm" })
            </td>
        </tr>
    }

</table>